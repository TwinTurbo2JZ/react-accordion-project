import React from "react";
import styles from "./css/Header.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <h1>Accordion Project Header</h1>
    </div>
  );
};

export default Header;
